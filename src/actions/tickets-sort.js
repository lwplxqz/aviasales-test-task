const cheaper = () => ({
    type: "SORT_CHEAPER"
})

const faster = () => ({
    type: "SORT_FASTER"
})

const optimal = () => ({
    type: "SORT_OPTIMAL"
})

export {
    cheaper,
    faster,
    optimal
}
